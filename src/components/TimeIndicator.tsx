
import React from 'react';

// Retained as no-op to avoid breaking imports; the old top-right clock chip
// clashed with the new layout and has been removed.
const TimeIndicator: React.FC = () => null;

export default TimeIndicator;
